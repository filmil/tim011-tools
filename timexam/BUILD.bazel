load("//build:rules.bzl", "mescc_binary")
load("//build:sdcc.bzl", "sdcc_z180_cpm_emu_run")

mescc_binary(
    name = "timexam",
    srcs = [
		"//lib/sprites:sprites.h",
        "timexam.c",
    ],
    deps = [
        "//lib/tim:timprint",
        "//lib/zxchars",
    ],
)

sdcc_z180_cpm_emu_run(
    name = "emu",
    binary=":timexam",
)
